class DayDataEntry {
  final String date;
  dynamic amount;
  dynamic count;

  DayDataEntry({
    required this.date,
    this.amount,
    this.count,
  });
}

class MonthDataEntry {
  final String month;
  dynamic amount;
  dynamic count;

  MonthDataEntry({
    required this.month,
    this.amount,
    this.count,
  });
}

class YearDataEntry {
  final String month;
  dynamic amount;
  dynamic count;

  YearDataEntry({
    required this.month,
    this.amount,
    this.count,
  });
}